# gate_hell — 논리 실험실 (Logic Lab)

논리 게이트를 배치하고 연결해서 회로의 동작을 관찰하는 tkinter 프로그램.

> **이 파일은 프로젝트의 유일한 기억이다.**
> 이 PC는 재시작하면 파일까지 스냅샷으로 초기화되므로, Claude의 대화 기록도 내부 메모리도 남지 않는다.
> 새 세션의 Claude는 이 문서만 읽고 상황을 파악한다. 규칙이 바뀌면 **반드시 여기에 반영하고 GitHub에 올린다.**

---

## 백업 리포지토리

| | |
| --- | --- |
| 저장소 | `https://github.com/<USERNAME>/gate-hell` ← `tools/repo.json` 에 실제 값 |
| 공개 범위 | Private |
| 방식 | **GitHub REST API** (git 미설치, 설치해도 재시작 시 사라짐) |
| 도구 | `tools/gh.ps1` — Claude가 실행한다 |
| 토큰 | `%LOCALAPPDATA%\gate_hell_token.txt` — **프로젝트 폴더 밖**이라 절대 커밋되지 않는다 |

교육 1회당 3~4시간. 끝나면 PC가 초기화되므로 매 세션 아래를 반복한다.

### 사용자가 하는 일 — 세션당 딱 하나

**토큰 붙여넣기.** 그게 전부다. 나머지는 Claude가 처리한다.

```
.\tools\gh.ps1 token github_pat_xxxxxxxx
```

토큰은 GitHub → Settings → Developer settings → **Fine-grained personal access tokens**에서 발급.
Repository access는 `gate-hell` 하나만, Permissions는 **Contents: Read and write** 하나만 준다.
토큰 문자열 자체는 브라우저에서 다시 볼 수 없으니, **휴대폰 메모장 등 PC 밖에 적어 둔다.**

### 세션 시작 — 복원

폴더가 비어 있으면 `gh.ps1` 자체가 없으므로 부트스트랩이 필요하다. 둘 중 하나:

- **A.** 리포 → **Code** → **Download ZIP** → 압축 풀어 `gate-hell-main` **내용물**을 프로젝트 폴더에 복사 (폴더째 말고 한 겹 벗겨서)
- **B.** 사용자가 토큰을 알려주면 Claude가 API로 직접 내려받는다 (`gh.ps1` 없이 PowerShell 한 줄)

`gh.ps1`이 살아 있는 상태면 그냥:

```
.\tools\gh.ps1 load
```

### 작업 중 · 세션 종료 — 백업

```
.\tools\gh.ps1 save "무엇을 했는지"
```

전 파일을 **커밋 1개**로 올린다. 파일이 몇 개든 상관없다.

### Claude가 할 일

- **세션 시작 시:** 폴더 상태를 먼저 확인한다. 이전 대화가 남아 있다고 가정하지 않는다.
  파일이 없거나 비어 있으면 복원 여부를 사용자에게 확인한다. 토큰이 없으면 요청한다.
- **작업 중:** **파일을 고칠 때마다 곧바로 `gh.ps1 save` 를 실행한다.** 모아 두었다가 나중에 올리지 않는다.
  사용자에게 시키지 말고 Claude가 실행한다. 정전이나 강제 재부팅이면 그때까지 작업이 전부 사라진다.
  커밋 메시지는 무엇을 바꿨는지 한 줄로 적는다.
- **세션 끝날 무렵:** 마지막 `save`를 실행하고, 아래 "진행 상황"을 갱신한 뒤 한 번 더 `save` 한다.
- **규칙이 추가·변경되면:** 이 CLAUDE.md를 즉시 갱신하고 `save` 한다. 여기 없는 규칙은 다음 세션에 존재하지 않는다.
- **토큰을 절대 프로젝트 폴더 안에 쓰지 않는다.** 출력에도 토큰 문자열을 그대로 남기지 않는다.

---

## 작업 방식 (필수)

- **코드는 사용자가 직접 작성한다. Claude는 코드를 쓰지 않는다.**
  - Claude의 역할: 개념 설명, 설계 상담, 버그 원인 찾기, 코드 리뷰, 힌트.
  - 명시적으로 "이거 짜줘"라고 요청받았을 때만 코드를 쓴다. 그 외에는 답이 아니라 방향을 제시한다.
- **이 PC는 사용자 소유가 아니다. 시스템을 건드리지 않는다.**
  - 작업 범위는 프로젝트 폴더(`Documents\gate_hell`) 안으로 한정한다.
  - 레지스트리·드라이브·프로세스·설치 목록 조회 등 시스템 조사를 하지 않는다. **읽기 전용이라도 하지 않는다.**
  - 설치·설정 변경·환경변수 수정 금지. 환경 정보가 필요하면 조사하지 말고 사용자에게 묻는다.

## 환경 제약 (필수)

- **Python 표준 라이브러리 + tkinter만 사용한다.**
- **`pip install` 금지.** 가상환경 생성도 하지 않는다. 재시작하면 어차피 사라진다.
- 확인된 환경: Python 3.11.7 (`C:\msys64\ucrt64\bin\python.exe`), tkinter / Tcl-Tk 8.6 정상 동작. git 미설치.

### 대체 수단

| 하고 싶은 것 | 쓸 것 |
| --- | --- |
| GUI | `tkinter`, `tkinter.ttk` |
| 그리기 / 애니메이션 | `tkinter.Canvas` (+ `widget.after()` 루프) |
| 이미지 | `tkinter.PhotoImage` (PNG/GIF만) |
| 데이터 저장 | `json`, `csv`, `sqlite3` |
| 계산 | `math`, `random`, `statistics`, `decimal`, `fractions` |
| 동시성 | `threading`, `queue` (UI 갱신은 반드시 메인 스레드에서 `after()`로) |

numpy, pandas, pygame, pillow, requests, matplotlib 등 서드파티 패키지는 사용하지 않는다.

## 구현 제약 (필수)

- **비트 연산자 사용 금지: `&`, `|`, `^`, `~`, `<<`, `>>` (및 `&=`, `|=`, `^=` 등 복합 대입).**
- **논리 연산은 `and`, `or`, `not` 세 개만 사용한다.**
  - XOR, NAND, NOR, XNOR 등은 이 셋을 조합해 **사용자가 직접 만든다.** Claude가 미리 만들어 주지 않는다.
- 여러 비트를 다룰 때는 정수 비트필드가 아니라 **불리언 리스트**(`[True, False, ...]`)로 표현한다.
  - 시프트 → 리스트 슬라이싱, 마스킹 → 인덱싱, 캐리 전파 → 루프.
- `int↔bin` 변환이 필요하면 `bin()`, `int(s, 2)`, 나눗셈·나머지(`//`, `%`)를 쓴다.

## 파일 구성

| 파일 | 내용 | 작성자 |
| --- | --- | --- |
| `CLAUDE.md` | 이 문서. 규칙과 복원 절차 | Claude |
| `프로젝트.md` | **과제 명세** — 교실 자동 냉난방/환기 시스템 (입력 6 / 출력 4) | 수업 |
| `classroom_hvac.py` | 명세 구현. 실행하면 진리표 출력 후 창이 열린다 | Claude |
| `00.py` | 사용자 작업 파일 | **사용자** |
| `code/` | 수업에서 받은 실습 파일과 템플릿 (원본, 수정하지 않음) | 수업 |
| `tools/gh.ps1` | GitHub 백업/복원 도구 | Claude |
| `tools/repo.json` | 리포 좌표 (owner/repo/branch) | Claude |

`classroom_hvac.py` 는 `code/05_invention_starter_2out.py` 의 구조를 따른다
(학생이 수정하는 부분 / GUI 프레임워크 2분할, `decide()`, `TEST_CASES`).

파일이 늘어나면 이 표에 추가한다. `gh.ps1 save`가 폴더 전체를 올리므로 파일 수 제한은 없다.

`tools/` 는 백업 인프라이므로 Claude가 관리한다. "코드는 사용자가 작성한다"는 규칙은 **논리 실험실 본체(`00.py` 등)**에 적용된다.

## 진행 상황

<!-- 세션이 끝날 때 여기에 한두 줄로 남긴다. 다음 세션의 Claude가 이어받는 지점이 된다. -->

- (2026-08-20) 규칙 확정, GitHub 백업 체계 수립 (`tools/gh.ps1`, 리포 `ENTIRANG/gate-hell`, 퍼블릭).
- (2026-08-20) `프로젝트.md` 구현 완료 → `classroom_hvac.py`.
  - `code/05_invention_starter_2out.py` 를 복사해 '학생이 수정하는 부분'만 교체. GUI 프레임워크는 템플릿 그대로.
  - 입력 6개(R, P, W, T1, T2, Q) / 출력 4개(C, H, A1, A2). `TEST_CASES` 64줄, 64/64 통과.
  - 명세에서 고친 것 세 가지:
    1. A1 에 `W` 추가 — 창문이 열려 있을 때만 "닫아라" 경고.
    2. A2 에 `not W` 추가 — 창문이 닫혀 있을 때만 "열어라" 경고.
    3. `hot = T1 and not T2`, `cold = T2 and not T1` — 온도 센서 모순 입력 차단.
       GUI 에서도 `EXCLUSIVE_INPUTS` 로 T1/T2 동시 선택을 막는다.
  - 수동 조작 모드 추가: R 이 꺼지면 사람이 냉난방을 직접 켜고 끈다. R 이 선택 신호인 2:1 멀티플렉서.
    `MANUAL_LABELS`, `EXCLUSIVE_MANUAL`, `final_outputs()` 참고. 진리표는 자동 논리만 담는다.
  - 불변 조건 전부 0건: C·H 동시 ON, A1·A2 동시 ON, 창문 상태 모순, T1·T2 동시일 때 온도출력 ON.

### 다음에 할 만한 것

- `프로젝트.md` 의 논리식을 불 대수 기호(`∧`, `∨`, `¬`)로 바꾸기 — 사용자가 요청했다가 보류됨.
- `진리표 보기` 창이 64줄이라 화면보다 길다. 템플릿 구조라 그대로 두었음. 스크롤이 필요하면 그때 논의.
