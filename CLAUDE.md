# gate_hell — 교실 자동 냉난방 · 환기 시스템

「불 대수와 논리게이트로 만드는 스마트 발명품」 팀 프로젝트. **과제 완료 상태.**

프로젝트 개요는 [README.md](README.md) 참고. 실행은 `cd 내작업` 후 `python classroom_hvac.py`.

---

## 작업 방식 (필수)

- **코드는 사용자가 직접 작성한다. Claude는 코드를 쓰지 않는다.**
  - Claude의 역할: 개념 설명, 설계 상담, 버그 원인 찾기, 코드 리뷰, 힌트.
  - 명시적으로 "이거 짜줘"라고 요청받았을 때만 코드를 쓴다.
- **이 PC는 사용자 소유가 아니다. 시스템을 건드리지 않는다.**
  - 작업 범위는 프로젝트 폴더 안으로 한정한다.
  - 레지스트리·드라이브·프로세스·설치 목록 조회 등 시스템 조사를 하지 않는다. **읽기 전용이라도 하지 않는다.**
  - 설치·설정 변경·환경변수 수정 금지. 환경 정보가 필요하면 조사하지 말고 사용자에게 묻는다.
- **`수업자료/` 는 선생님이 주신 원본이다. 수정하지 않는다.**

## 환경 제약 (필수)

- **Python 표준 라이브러리 + tkinter만 사용한다.**
- **`pip install` 금지.** 가상환경도 만들지 않는다. 재시작하면 어차피 사라진다.
- 확인된 환경: Python 3.11.7 (`C:\msys64\ucrt64\bin\python.exe`), tkinter / Tcl-Tk 8.6. git 미설치.
- numpy, pandas, pygame, pillow, requests, matplotlib 등 서드파티 패키지는 쓰지 않는다.

## 구현 제약 (필수)

- **비트 연산자 사용 금지: `&`, `|`, `^`, `~`, `<<`, `>>` (및 복합 대입).**
- **논리 연산은 `and`, `or`, `not` 세 개만 사용한다.**
  XOR, NAND, NOR 등이 필요하면 이 셋을 조합해 직접 만든다.
- 여러 비트를 다룰 때는 정수 비트필드가 아니라 **불리언 리스트**로 표현한다.

## 휘발성 경고 (필수)

- **이 PC는 재시작하면 파일까지 스냅샷으로 초기화된다.** 이 파일도, 소스도, Claude의 메모리도 예외 없이 사라진다.
- 같은 PC 안의 다른 폴더로 옮기는 것은 백업이 아니다.
- 백업 위치: **https://github.com/ENTIRANG/gate-hell** (public)
- git 이 설치되어 있지 않으므로 브라우저로 주고받는다.
  - **복원**: 리포 → `Code` → `Download ZIP` → 압축 풀어 `gate-hell-main` **내용물**을 프로젝트 폴더에 복사 (폴더째 말고 한 겹 벗겨서)
  - **백업**: 리포 → `Add file` → `Upload files` → 파일 드래그 → `Commit changes`
- 예전에는 `tools/gh.ps1` 로 Claude 가 API 커밋을 했으나, 과제 종료 시 사용자 요청으로 삭제했다.
  다시 필요하면 GitHub 토큰(classic, `repo` 스코프)을 받아 새로 만든다.

## 커밋하지 않는 것

- `내작업/발표대본.txt` — 사용자 지시로 커밋 제외
- `수업자료/발명실 설문.png` — 사용자 지시로 커밋 제외 및 삭제
- `__pycache__/`

## 진행 상황

- (2026-08-20) 과제 완료.
  - `프로젝트.md` 명세를 `내작업/classroom_hvac.py` 로 구현. 입력 6 / 출력 4, `TEST_CASES` 64줄 전부 통과.
  - `수업자료/code/05_invention_starter_2out.py` 를 복사해 '학생이 수정하는 부분'만 교체.
    GUI 프레임워크 240줄은 템플릿과 동일 (한국어 문구 6줄만 "두 출력" → "네 출력").
  - 명세에서 고친 것: `A1` 에 `W` 추가, `A2` 에 `not W` 추가, `hot`/`cold` 로 온도 센서 모순 차단.
  - 수동 조작 모드 추가 — `R` 이 선택 신호인 2:1 멀티플렉서.
  - 발표 대본 작성 (1인 발표, 시연 9단계 실측 검증). 커밋하지 않음.
  - 폴더를 `수업자료/` 와 `내작업/` 으로 분리.
